# capdi-static

This is a simple static preview of the CAPDI homepage, created for an initial visual review.

Open `index.html` in your browser to view the site.

To iterate:
- Replace placeholder images in `index.html` with real photos.
- Update the Formspree form action in the contact form.
